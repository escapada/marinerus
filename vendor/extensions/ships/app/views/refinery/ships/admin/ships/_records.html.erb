<%= render 'refinery/admin/search_header', :url => refinery.ships_admin_ships_path %>
<div class='pagination_container'>
  <% if @ships.any? %>
    <%= render 'ships' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
