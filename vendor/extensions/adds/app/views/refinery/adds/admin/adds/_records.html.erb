<%= render 'refinery/admin/search_header', :url => refinery.adds_admin_adds_path %>
<div class='pagination_container'>
  <% if @adds.any? %>
    <%= render 'adds' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
